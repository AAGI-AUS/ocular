% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch-processing.R
\name{batch_rs}
\alias{batch_rs}
\title{Build spectral index time series for multiple sites}
\usage{
batch_rs(
  sites,
  index_name = "EVI2",
  source = "auto",
  x_metres = 1000L,
  y_metres = NULL,
  max_cloud_cover = 50L,
  params = NULL,
  pipeline = NULL,
  cache = TRUE,
  cache_dir = NULL,
  pipeline_cache_key = NULL,
  retries = 3L,
  cloud_relax = 80L,
  on_error = c("skip", "stop"),
  quiet = FALSE
)
}
\arguments{
\item{sites}{A \code{data.frame} with columns \code{id}, \code{longitude},
\code{latitude}, \code{start_date}, and \code{end_date}; one row per
site and date range.}

\item{index_name}{Index to calculate; see \code{get_rs()}.}

\item{source}{Image source passed to \code{get_rs()}. The default is
\code{"auto"}.}

\item{x_metres, y_metres}{Width and height of the analysis area in metres.
If \code{y_metres} is \code{NULL}, \code{x_metres} is used for both.}

\item{max_cloud_cover}{Maximum scene-level cloud cover for the ordinary
retrieval attempts.}

\item{params}{Optional complete or partial list of settings accepted by
\code{rs_params()}, passed to each \code{get_rs()} call.}

\item{pipeline}{Post-retrieval function that accepts an \code{ocular} object
and returns a \code{data.frame} with \code{date} and \code{value}. By
default, \code{boundary_delineation()} is followed by
\code{as_time_series()}. If \code{multiple_areas = TRUE}, the default
function uses the \code{union} column as \code{value}.}

\item{cache}{Whether to read and write the on-disk cache. See
\code{\link{clear_rs_cache}}.}

\item{cache_dir}{Cache location. \code{NULL} uses the versioned per-user
cache directory.}

\item{pipeline_cache_key}{Stable character identifier for a custom
\code{pipeline}. A custom function is not cached unless this is supplied,
because its body does not identify captured values reliably.}

\item{retries}{Number of retrieval attempts per site. The post-retrieval
function is evaluated once, after retrieval succeeds.}

\item{cloud_relax}{Cloud-cover ceiling considered on the final attempt when
\code{retries > 1}. The final ceiling is the greater of this value and
\code{max_cloud_cover}.}

\item{on_error}{\code{"skip"} (default) records the failure and continues;
\code{"stop"} aborts the sweep.}

\item{quiet}{Whether to suppress per-site progress messages.}
}
\value{
A \code{data.frame} with \code{id}, \code{index}, \code{source},
  \code{date}, and \code{value}. Failed sites are absent and recorded in the
  \code{"failures"} attribute with their \code{id}, \code{index}, and
  \code{reason}.
}
\description{
For each row of \code{sites}, retrieves scenes with \code{get_rs()} and
passes the resulting \code{ocular} object to a post-retrieval function. The
default function runs field boundary delineation and creates a time series.
Per-site results are combined in one long \code{data.frame}. Completed
results can be cached on disk, and failed retrievals can be attempted again.
}
\examples{
\dontrun{
sites <- data.frame(id = c("a", "b"),
                    longitude = c(-97.29971, -99.17693),
                    latitude  = c(30.60044, 29.40344),
                    start_date = c("2019-03-26", "2019-03-11"),
                    end_date   = c("2019-08-20", "2019-08-05"))
ts <- batch_rs(sites, index_name = "GNDVI", x_metres = 1200)
attr(ts, "failures")
}
}
\seealso{
\code{\link{get_rs}}, \code{\link{as_time_series}},
  \code{\link{clear_rs_cache}}
}
